# Зависимости библиотеки FunPayAPI, которая общается с FunPay.
# Ядру бота (склад, выдача, панель, doctor, backup) они не нужны — оно
# работает на одной стандартной библиотеке.
beautifulsoup4>=4.11
lxml>=5.3
requests>=2.28
requests_toolbelt>=0.10
urllib3>=1.26

# Сама FunPayAPI ставится не через pip: она не опубликована в PyPI и не
# является отдельным пакетом, а лежит папкой внутри проекта FunPayCardinal.
# Её достаёт scripts/fetch_funpayapi.sh — он вызывается в build-команде.
